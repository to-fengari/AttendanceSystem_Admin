<Window x:Class="prototype.View.list"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d">
    
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Department Attendees" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Department Name:" FontWeight="Bold"/>
            <TextBlock x:Name="DepartmentNameText" Margin="0,0,0,10"/>
            
            <ListView x:Name="DetailsListView" Margin="0,10,0,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Student Name" DisplayMemberBinding="{Binding Detail}" Width="300"/>
                        <GridViewColumn Header="Student ID">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
